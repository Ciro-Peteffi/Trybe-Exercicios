const alpha = 60;
const beta = 39;
const gama = 0;

if (alpha <= 0 || beta <=0 || gama <= 0) {
    console.log("valor inválido")
} else if (alpha + beta + gama === 180) {
    console.log (alpha + beta + gama === 180);
}      else {
    console.log(alpha + beta + gama === 180);
}